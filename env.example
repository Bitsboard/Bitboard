# Database
DATABASE_URL="postgresql://username:password@localhost:5432/bitboard"

# Bitcoin Price API
COINGECKO_URL="https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=cad"

# Lightning Network Backend (for escrow functionality)
LN_BACKEND="mock" # Options: "mock", "lnd", "cln", "lnbits"
LN_HOST="localhost"
LN_PORT="10009"
LN_MACAROON=""
LN_CERT=""

# Authentication (for future implementation)
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# External Services
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER=""

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="bitsbarter"
NEXT_PUBLIC_APP_DESCRIPTION="Bitcoin-native local classifieds"

# Build/branch indicators (used to show staging badge automatically)
# NEXT_PUBLIC_BRANCH is inferred from platform env (CF_PAGES_BRANCH/VERCEL_GIT_COMMIT_REF) in next.config.mjs
# Set NEXT_PUBLIC_ENV to "staging" locally if you want to see the badge in dev
# NEXT_PUBLIC_BRANCH="staging"
# NEXT_PUBLIC_ENV="staging"
